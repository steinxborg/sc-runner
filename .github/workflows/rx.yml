name: rx
on:
  workflow_dispatch:

jobs:
  fetch_data:
    runs-on: ubuntu-latest
    env:
      REPO_FOLDER: rx-repo          # Local folder name for the cloned repo
      SCRIPT_NAME: rx.py            # Script to run
      OUTPUT_FILE: rx.json          # Output file generated by the script
    steps:
      # Checkout public repo (workflow runs here)
      - uses: actions/checkout@v5

      # Set up Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      # Install dependencies
      - name: Install dependencies
        run: pip install httpx pycryptodome

      # Clone the private repo
      - name: Clone repo
        run: |
          git clone https://${{ secrets.SCRAPER_TOKEN }}@github.com/${{ secrets.SCRAPER_REPO }} $REPO_FOLDER

      # Run the Python script
      - name: Run Python script
        run: |
          cd $REPO_FOLDER
          python $SCRIPT_NAME

      # Commit and push output
      - name: Commit and push output
        run: |
          cd $REPO_FOLDER
          git config --global user.name "actions-user"
          git config --global user.email "actions@github.com"
          
          git add $OUTPUT_FILE
          
          # Commit only if changes exist
          git diff --cached --quiet || git commit -m "Update $OUTPUT_FILE"
          
          # Push changes
          git push
